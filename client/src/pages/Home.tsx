/* Style preservation: This route hosts the unchanged source portfolio; its dark cinematic visual language lives in Portfolio.jsx. */
// @ts-expect-error The preserved source component intentionally remains JavaScript.
import Portfolio from "../Portfolio";

export default function Home() {
  return <Portfolio />;
}
